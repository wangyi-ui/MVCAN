All optimizer gradients are cleared with zero_grad(set_to_none=True) before every Phase-A and Phase-B batch and after each complete phase.
Phase-specific gradient families are asserted and nonfinite gradients fail closed.
No gradient leakage remains across phase boundaries.
