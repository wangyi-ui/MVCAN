P1-A0 FORMAL MAIN-EXPERIMENT PROTOCOL MATRIX

Native preparation (each field RESOLVED; source: release_core/config/native.py)
Dataset       N     V  K  view dimensions                 native seed  batch  init  T1  T2  native epochs  lr      lambda1
Caltech-6V    1400  6  7  48,40,254,1984,512,928          5            256    200   2   100 1000           1e-4    0.01
MSRC-v1       210   5  7  24,576,512,256,254              20           256    200   2   100 1000           1e-4    0.01
BDGP          2500  2  5  1750,79                         1            256    200   2   100 1000           1e-4    10

The native 5/20/1 values are config training seeds, never closed-loop epochs.
Native preparation produces the same-condition initial checkpoint/state.

Formal seed matrix (RESOLVED)
training seeds: 20, 30, 50
weak-quality realization seed: 20, fixed across training seeds
label split seed: 20, fixed across training seeds
KMeans random_state and DataLoader/order seed: current training seed

Weak quality, SNR = 2.5 dB (RESOLVED)
Caltech-6V: exactly 3/6 views per sample; mask logical SHA256 e6250535db6e33b9263102cf335fcf9e8552a24cae4b70ec5a94887f4e58bf0b
MSRC-v1: 105 samples x2 + 105 samples x3 corrupted views; mask logical SHA256 d64f5fbf95518e5a77e44a1fe18a15dee9924b8c2a341e930f86f20ce5ac9e9d
BDGP: exactly 1/2 views per sample; mask logical SHA256 a3c882c29f2f5064d552e60bdc1a2d585764317e7ba78aa055cd1c3c22e5de07

Sparse labels (RESOLVED): 2/class, label seed 20; totals Caltech=14, MSRC=14, BDGP=10.

For every dataset x training_seed, R2/R3 are freshly computed from that
same-condition native-prepared state (q_local, q_aligned, U_cycle, y_gen,
PredRelation, and balance) exactly once, then detached/frozen throughout R5.

R5 alternating schedule (RESOLVED): 20 epochs; Phase A -> target refresh ->
Phase B. T2=100 uses zero-based epoch % T2 == 0: epoch 0 refreshes; there
are no further in-loop refreshes in epochs 0..19. A final native refresh runs
once after R5. Final predictions are its second-pass KMeans IDs, not q argmax.

Main arms: BASE; OURS_TRUE_U.  Ablation-only arms: TRUE_UNIFORM; SHUFFLE_U.
